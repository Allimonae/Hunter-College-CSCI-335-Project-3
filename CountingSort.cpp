#include <algorithm>
#include <unordered_map>
#include "CountingSort.hpp"

void countingSort(const std::string& header, std::vector<int> data){
    std::unordered_map<int, int> countHash;
    int unique = 0;
    // for (auto i : data){
    //     if (countArray.find(i) == countArray.end()){
    //         unique++;
    //     }
    //     countArray[i]++;
    // }
    std::cout << unique;
    for (auto j : countHash){
        std::pair<int, int> 
    }
    std::vector<std::pair<int, int>> 
}